-> preorder but two cases (root, no root)
-> if root: root.val + leftsubTree(no root) + rightSubTree(no root)
-> if no root: I can take SubTrees root or skip SubTrees root
               So max(leftSubTree) + max(rightSubTree)